#Boolean Indexing in Pandas...............................
# In boolean indexing, we will select subsets of data based on the actual values of the data in the DataFrame
# and not on their row/column labels or integer locations.
# In boolean indexing, we use a boolean vector to filter the data.

# In boolean indexing, we can filter a data in four ways:
#
# Accessing a DataFrame with a boolean index
# Applying a boolean mask to a dataframe
# Masking data based on column value
# Masking data based on an index value

# 1 Accessing a DataFrame with a boolean index:
# using loc[] and iloc[] function